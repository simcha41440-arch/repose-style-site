# תיקון ההתראה "פגיעות אבטחה בפרויקטים שלך" (rls_disabled_in_public)

ההתראה מ-Supabase אומרת שיש טבלה אחת (או יותר) בפרויקט **שאין עליה
Row Level Security פעיל** - כלומר, מבחינה טכנית, כל מי שיש לו את
כתובת ה-URL וה-anon key של הפרויקט (שניהם ממילא גלויים בקוד הצד-לקוח
של כל אתר) יכול תיאורטית לקרוא/לכתוב ישירות לטבלה הזאת, בלי לעבור
דרך ה-API של האתר.

זו **הגדרה בצד מסד הנתונים** ב-Supabase, ולכן אי אפשר לתקן אותה רק
על ידי שינוי קבצים באתר - צריך להריץ SQL פעם אחת בדשבורד.

## מה לעשות (חד פעמי, 2 דקות)

1. להיכנס ל-**supabase.com/dashboard** → הפרויקט שלך (רפאוז סטייל).
2. **SQL Editor** → **New query**.
3. לפתוח את הקובץ `FIX-RLS.sql` שמצורף בזיפ הזה, להעתיק את כל
   התוכן, להדביק, וללחוץ **Run**.
4. זהו. זה מדליק RLS על כל הטבלאות (גם אלה שכבר מאובטחות - הרצה
   כפולה לא מזיקה), בלי למחוק או לשנות שום נתון קיים.

אחרי ההרצה, כדאי לחזור לדף ההתראה של Supabase וללחוץ "Re-run" /
לרענן - היא אמורה להיעלם תוך כמה דקות.

**לא צריך להעלות מחדש (Redeploy) את האתר בוורסל בשביל התיקון הזה** -
זו פעולה שקורית רק במסד הנתונים, לא בקוד. `schema.sql` המצורף כבר
כלל את כל שורות ה-RLS מההתחלה; ההתראה קרתה כנראה כי חלק מהשורות
האלה (בעיקר הטבלאות שנוספו מאוחר יותר: `login_attempts`,
`admin_audit_log`, `rate_limit_events`) לא הורצו בפועל בדשבורד.
