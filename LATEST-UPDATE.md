# מה עודכן בזיפ הזה

## פס הטעינה הזהוב עולה גם במעבר אחורה בין עמודים
עד עכשיו הפס הזהוב למעלה היה מהבהב רק בלחיצה על כפתור. עכשיו הוא עולה
בכל ניווט אמיתי בין עמודים - כולל לחיצה על "חזרה" בדפדפן (או "קדימה"),
כי הוא מופעל ישירות מתוך `showView()` שרץ בכל מקרה. **אין SQL חדש, אין
משתני סביבה חדשים.**

## התחברות והרשמה באזור האישי
נוסף חשבון לקוח אמיתי: טאב "כניסה" וטאב "הרשמה" בראש עמוד "האזור האישי
שלי", עם דוא"ל+סיסמה (הסיסמה נשמרת מוצפנת, לא כטקסט גלוי). לקוח מחובר
רואה "שלום, [שם]" וכפתור התנתקות. חיבור עובד גם אחרי סגירת הדפדפן
(נשאר מחובר 30 יום). בנוסף, כמו באתרים גדולים:
- כפתור "עין" להצגת/הסתרת הסיסמה בזמן ההקלדה.
- "שכחתי סיסמה" ששולח מייל עם קישור לאיפוס (בתוקף לשעה), ואחרי קביעת
  סיסמה חדשה המשתמש מתחבר אוטומטית.

**כדי שזה יעבוד צריך:**
1. להריץ את `CUSTOMER-AUTH-SETUP.sql` פעם אחת ב-Supabase (SQL Editor) -
   אם כבר הרצתם גרסה קודמת של הקובץ הזה, פשוט הריצו את הגרסה המעודכנת
   שוב, היא בטוחה להרצה חוזרת ומוסיפה רק את מה שחסר.
2. להוסיף משתנה סביבה חדש ב-Vercel: `CUSTOMER_SESSION_SECRET` (כל מחרוזת
   אקראית וארוכה - כמו ה-`ADMIN_SESSION_SECRET` שכבר קיים, רק מחרוזת
   אחרת), ואז לעשות Redeploy.
3. שליחת מייל האיפוס משתמשת באותה הגדרת Resend שכבר יש לכם (עבור מיילי
   הזמנות) - `RESEND_API_KEY` ו-`RESEND_FROM_EMAIL`. אם אלה כבר מוגדרים,
   אין צורך בעוד כלום.

**שימו לב:** זה מוסיף קובץ API חדש (`api/auth.js`), מה שמעלה את מספר
הפונקציות בפרויקט ל-12 - בדיוק התקרה של תוכנית ה-Hobby בוורסל שכבר
פגעתם בה בעבר (ראו למטה). זה אמור עדיין לעבוד, אבל אם הפריסה (Deploy)
נכשלת עם שגיאה על מספר הפונקציות - תגידו לי ואני אאחד שני קבצי API
קיימים לאחד כדי לפנות מקום.

לב"ל: בשלב הזה החשבון משמש רק לזיהוי/כניסה - ההזמנות, המועדפים
והעגלה שמוצגים באזור האישי עדיין נשמרים לפי הדפדפן (כמו קודם) ולא
משויכים עדיין לחשבון. אם תרצו שהזמנות יופיעו אוטומטית לפי החשבון
המחובר (גם ממכשיר אחר), זה שינוי נוסף שאפשר לעשות בנפרד.

## "הסט כולל" מתעדכן לפי הגודל שנבחר
כשלוחצים על **"יחיד"** בעמוד המוצר, הרשימה מתחת ("הסט כולל") מתעדכנת
מיד ומציגה כמות **1** מכל פריט במקום 2 (למשל "1 סדין עם גומי מלא"
במקום "2 סדין עם גומי מלא", וכן ציפיות הכרית). לוחצים חזרה על "זוג
יחידים" - חוזר אוטומטית להציג 2 מכל פריט.

**אין SQL חדש, אין שינויים אחרים.** רק להעלות (Redeploy) את הזיפ.

## תזכורות מעדכונים קודמים
- תיקון פריסת וורסל (מגבלת 12 פונקציות) - כבר בזיפ הזה.
- אזל מהמלאי, בחירת יחיד/זוג במחצית מחיר, מבצעים, עריכת תוכן.
- קופונים חד-פעמיים, כתובת מלאה בהזמנות, "סקירה כללית", ייצוא CSV.
- כל ה-SQL שנצבר עד כה נמצא בקובץ `ALL-SQL-SETUP.sql` שבזיפ, אם עדיין
  לא הרצתם אותו.
