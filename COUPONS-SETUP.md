# תוספת: קופונים בעמוד הניהול

## מה עשיתי
הוספתי טאב חדש בעמוד הניהול בשם **"קופונים"**. אפשר ליצור שם קודי
הנחה (קוד + אחוז הנחה + מינימום קנייה אופציונלי), וכל קוד פעיל מופיע
מיד בשדה "קוד קופון" בעמוד הקופה באתר - **נשמר בשרת (Supabase)**,
בדיוק כמו מוצרים והזמנות, כך שהוא תקף לכל הגולשים ולא רק בדפדפן שבו
נוצר.

השדה "קוד קופון" בקופה כבר היה קיים בעיצוב, אבל לא היה מחובר לשום
דבר בפועל - עכשיו הוא אמיתי: לקוח שמקליד קוד תקף רואה שורת הנחה
בסיכום ההזמנה, והסכום הסופי מתעדכן בהתאם.

## מה תצטרך לעשות (שני צעדים, בדיוק כמו בפעם הקודמת)

**שלב 1 - להריץ SQL חדש ב-Supabase:**
1. supabase.com/dashboard → הפרויקט שלך → **SQL Editor** → **New query**.
2. תדביק ותריץ (Run):

```sql
create table if not exists coupons (
  id uuid primary key default gen_random_uuid(),
  code text not null unique,
  percent numeric not null check (percent > 0 and percent <= 100),
  min_subtotal numeric,
  active boolean not null default true,
  created_at timestamptz not null default now(),
  updated_at timestamptz
);
alter table coupons enable row level security;
```

**שלב 2 - להעלות את הזיפ הזה לוורסל (Redeploy).**

זהו. אחרי זה תראה טאב "קופונים" חדש בסיידבר של עמוד הניהול. שם אפשר
להוסיף קוד (לדוגמה SUMMER10, 10%), לסמן פעיל/כבוי, ולמחוק. הקוד יהיה
זמין מיד בקופה באתר לכל הלקוחות.

## הערה לגבי כיבוי קופון
בדיוק כמו בטבלת "מוצרים עם override פעיל", הטבלה בעמוד הניהול מציגה
רק קופונים פעילים. אם תכבו קופון הוא ייעלם מהרשימה; כדי להחזיר אותו
לפעולה פשוט תוסיפו שוב את אותו הקוד עם "פעיל" מסומן.
